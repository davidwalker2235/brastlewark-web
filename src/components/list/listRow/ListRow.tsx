import React, { FC } from 'react';
import { ListRows } from '../../../interfaces/appInterfaces';

const ListRow: FC<ListRows> = ({itemData, onClickRow, listType}) => {

  return (
    <div>
      listRow
    </div>
	)
}

export default ListRow;