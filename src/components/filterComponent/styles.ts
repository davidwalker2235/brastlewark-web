import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
    list: {
        width: 250,
    },
        fullList: {
        width: 'auto',
    },
});

export default styles;